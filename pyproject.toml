[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "comic-topic-radar"
version = "0.1.0"
description = "漫剧选题信号领域契约"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
